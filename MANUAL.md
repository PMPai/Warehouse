# 設備管理系統 使用手冊

> 系統名稱：設備管理系統（warehouse）
> 版本：1.2（定稿）
> 更新日期：2026-08-30

---

## 目錄

0. [系統安裝與啟動](#0-系統安裝與啟動)
   - [登入系統](#登入系統)
   - [安裝 Agent Skill（外部用戶）](#安裝-agent-skill外部用戶)
1. [系統主要功能](#1-系統主要功能)
2. [使用方式](#2-使用方式)
   - 2.1 [儀表板的使用](#21-儀表板的使用)
   - 2.2 [資料／記錄的輸入方式](#22-資料記錄的輸入方式)
   - 2.3 [自然語言查詢](#23-自然語言查詢)

---

## 0. 系統安裝與啟動

### 首次安裝

```bash
# 1. 複製專案到伺服器
# 2. 安裝依賴
npm install

# 3. 設定環境
cp .env.example .env
# 編輯 .env 設定 host/port（開發用 127.0.0.1:8088）

# 4. 從舊 Excel 匯入真實資料（首次才需要）
node scripts/migrate.mjs
# 會讀取 2026年資產明細表08-11.xls，匯入所有設備/耗材/案場

# 5. 啟動系統
./start.sh   # 或 npm start
# 開啟 http://127.0.0.1:8088
```

### 日常啟動

```bash
./start.sh   # UNIX：雙擊即可
```

Windows：雙擊 `start.bat`（獨立視窗常駐；關閉視窗即停止服務）。

### 登入系統

開啟系統網址（正式部署：`http://168.144.98.68:8081`，亦可 `http://ai.jines.com:8081`）後，第一個看到的是**登入頁**。

- **帳號**：`admin`（系統唯一帳號；登入後可看、可改所有記錄，無權限分級）
- **密碼**：目前為 `Jines2355`，硬編碼於 `src/auth.js`（`ADMIN_USER`/`ADMIN_PASS`）；改密碼＝改該檔並重啟服務
- 登入成功後瀏覽器取得 session cookie（HttpOnly），在登出或服務重啟前不用重登
- 頁面右上角「登出」按鈕可随时登出
- `/api/health` 為唯一免認證端點（供監控用）

#### Agent／API 呼叫認證（Basic Auth）

AI Agent（skill）與腳本呼叫 API 不走登入頁，每個 request 附 Basic Auth：

```bash
curl -u "admin:Jines2355" http://168.144.98.68:8081/api/dashboard
```

skill 規範：帳密放在**使用者自己電腦**的 `~/.config/warehouse/credentials`（單行 `user:password`，`chmod 600`），由管理者線下提供；**絕不寫進 skill 檔、對話或 repo**：

```bash
curl -u "$(cat ~/.config/warehouse/credentials)" http://168.144.98.68:8081/api/items?q=鑽機
```

收到 `401` 表示憑證錯誤或憑證檔未設定。

> ⚠ 目前為 HTTP 明文傳輸。系統上公網前應加 TLS（如 nginx 反代 + Let's Encrypt），否則帳密可被攔截。

### 安裝 Agent Skill（外部用戶）

兩個 skill（`warehouse-query` 查詢、`warehouse-entry` 輸入）可安裝在任何支援 **Agent Skills** 格式的 AI agent（opencode、Claude 相容平台等）。**只需兩步，直接對你自己的 agent 說：**

#### 步驟 1：寫入憑證

跟你本機的 agent 說：

> 請在我電腦建立 `~/.config/warehouse/credentials`，內容為單行 `admin:Jines2355`，並設權限 600。

agent 會執行：
```bash
mkdir -p ~/.config/warehouse
printf 'admin:Jines2355' > ~/.config/warehouse/credentials
chmod 600 ~/.config/warehouse/credentials
```

#### 步驟 2：安裝 skill

跟你本機的 agent 說：

> 請從 `https://github.com/PMPai/Warehouse` 只安裝 `.opencode/skills` 下的兩個 skill（`warehouse-query`、`warehouse-entry`）到你的 skills 目錄，不要整個專案。

agent 會把兩個 skill 裝到對應目錄：

| 平台 | 全域安裝路徑 |
|---|---|
| opencode | `~/.config/opencode/skills/` |
| Claude 相容 | `~/.claude/skills/` |
| 通用 Agent Skills | `~/.agents/skills/` |

完成。之後：

- **輸入**：把進出倉單據照片交給 agent，說「用輸入流程處理」
- **查詢**：直接口語提問（如「26-023 還有哪些設備沒回」「二重管庫存多少」）
- 中台位址預設 `http://168.144.98.68:8081`；搬位址時用環境變數 `WAREHOUSE_API` 覆蓋，或直接告訴 agent 新位址
- 若 agent 回報 `401`：重做步驟 1；連線失敗：檢查網路或聯絡管理者開防火牆

### 重置資料庫（測試用）

```bash
node scripts/init-db.js    # 重建空 DB
node scripts/migrate.mjs   # 重新匯入
node scripts/seed.js       # 匯入測試假資料（不含真實資料）
```

### 已匯入資料量

| 類型 | 數量 |
|---|---|
| 設備品項 | 494 |
| 設備個體 | 1334 台 |
| 耗材 SKU | 117 |
| 案場 | 147 |

> 現況：設備個體為 1335 台（驗證期間依「無編號開新號」規則新增 1 台）。

---

## 1. 系統主要功能

設備管理系統是一套管理設備與耗材進出倉的工具，主要解決以下問題：

- **快速登記進出倉**：不再需要手動翻閱手寫單、逐一判讀，系統透過 AI Agent 自動辨識照片內容，經人工確認後即可入帳
- **完整物料清單**：所有設備（有編號，如鑽機 #21）和耗材（數量管理，如二重管 3M）統一管理
- **設備流動軌跡**：每台設備經歷過哪些案場、何時出何時回，一目了然
- **儀表板**：隨時掌握哪些設備在外沒回、什麼待修、什麼耗材快沒了
- **報表匯出**：依案號、品名、類型分組統計，可匯出 CSV
- **人工調整**：所有記錄都可編輯或刪除，並留下修改日誌（audit_log）
- **原始單據照片**：進出單可上傳並關聯原始照片，於單據詳情檢視（輸入 Agent 確認寫入後自動附上）

### 系統三大部分

| 部分 | 說明 |
|---|---|
| **中台（本系統）** | 網頁介面＋資料庫，所有資料儲存於此 |
| **輸入 Agent** | AI 助手，讀手寫單照片→辨識→確認後自動寫入中台 |
| **查詢 Agent** | AI 助手，用自然語言問問題，自動查詢中台回答 |

---

## 2. 使用方式

### 2.1 儀表板的使用

開啟系統後，第一個看到的頁面就是儀表板。

#### 儀表板看到什麼？

儀表板分為三個區塊：

**① 整體概況卡片**（頁面最上方）

| 卡片 | 意思 | 顏色意義 |
|---|---|---|
| 設備總數 | 系統中所有設備的數量 | — |
| 在庫 | 目前在倉庫的設備 | 綠色 |
| 在外 | 借出未回的設備 | 藍色 |
| 待修 | 故障送修中的設備 | 橙色 |
| 超過30天 | 在外超過30天，該追回了 | 紅色＝需注意 |
| 低庫存 | 耗材低於安全庫存，該補貨了 | 紅色＝需注意 |

> 💡 **5秒看法則**：首屏掃一眼紅色數字，就知道今天要先處理什麼。

**② 使用最多的器材列表（TOP 10）**

列出異動最頻繁的設備，按異動次數排序。點擊品名可看該設備的歷史軌跡。

**③ 耗材本週列表**

顯示本週有進出的耗材：目前庫存、本週進了多少、出了多少。數字紅色＝低於安全庫存。點擊可調整庫存。

**④ 最近異動**

最新10筆進出記錄，點擊可看原始進出單。

#### 側邊選單

| 選單 | 功能 |
|---|---|
| 儀表板 | 回到首頁 |
| 快速開單 | 手動建立進出單 |
| 進出單 | 查看所有進出單，可搜尋/編輯/刪除 |
| 設備 | 查看所有設備，可篩選/編輯/刪除 |
| 耗材庫存 | 查看耗材庫存，可調整/編輯/刪除 |
| 異動紀錄 | 查看所有異動紀錄，可編輯/刪除 |
| 報表 | 統計報表，可匯出 CSV |
| 設備設置 | 新增器材、設定初始庫存、調整數量 |

---

### 2.2 資料／記錄的輸入方式

系統有三種方式輸入記錄：

#### 方式一：快速開單（手動輸入）

適用於：你手上有正式進出單，或現場直接操作時。

**操作步驟：**

1. 點左側「快速開單」
2. 選擇**類型**：
   - 出倉：從倉庫發料到案場
   - 進倉：新購首次入庫
   - 回倉：案場結束設備送回
   - 轉移：A 工地直接送 B 工地（不經倉庫）
   - 報廢：設備損壞永久除役
   - 送修：故障送修
   - 修回：修好回倉
3. 填**日期**（預設今天）
4. 填**案號**（可從清單選，或直接打字）
5. 填**借用人**（簽名人）

> ⚠ 如果選「轉移」，會多出以下欄位：
> - **來源案號**（A 案場）
> - **目的地案號**（B 案場）
> - **移交人**（A 案場負責人）
> - **接收人**（B 案場負責人）
>
> 轉移必須填寫雙方負責人。

6. 在「明細」區塊，點「＋ 新增明細」
7. 在品名欄輸入文字（會自動搜尋，如打「鑽」會列出鑽機），**從清單點選正確品名**
8. 填數量、編號（設備才需要，耗材不用）、狀況備註（如「故障」「壞」）
9. 可重複新增多筆明細
10. 點「確認入帳」直接寫入，或「存草稿」稍後再確認

> 💡 **品名一定要從清單選**，不能直接打字不選。系統需要知道你說的「洗網機」對應到哪個器材。

#### 方式二：AI Agent 照片輸入（推薦）

適用於：現場人員拍照回傳的手寫單。

**操作步驟：**

1. 收到現場人員傳來的手寫單照片
2. 在 AI Agent 平台（opencode）中使用 `warehouse-entry` 技能
3. 把照片交給 Agent

Agent 會自動：
- 辯識照片內容（日期、案號、品名、數量、編號）
- **推斷異動類型**（出倉/回倉/轉移/報廢/送修…）
- 匹配品名和別名（如「洗網機」→「洗車機」）
- 列出草稿讓你確認

4. **確認步驟（重要）**：
   - 先確認**異動類型**對不對（Agent 會推斷，但不一定準）
   - 若類型不確定，Agent 會列出選項讓你選
   - 逐一確認品名、數量、編號
   - 無編號的設備，系統會自動開新編號
5. 確認無誤後，Agent 自動呼叫 API 寫入中台
6. Agent 回報：單號、每項設備/耗材的變化、新開的編號

> 💡 Agent 會把同一張照片上同時有「出」和「回」的，**自動拆成兩張單**處理。

#### 方式三：設備設置（初始建檔）

適用於：新增器材、調整庫存。

> 系統已透過遷移腳本（`node scripts/migrate.mjs`）從舊 Excel 匯入所有現有器材（494 設備品項、1334 台、117 耗材 SKU）。以下操作用於之後的新增與調整。

1. 點左側「設備設置」
2. **新增器材**：
   - 選類型（設備＝個體管理有編號；耗材＝數量管理）
   - 填品名、規格、別名（如洗網機=洗車機，用逗號分隔）、代碼、單位、價格
   - 若是耗材，可設定初始庫存數量和安全庫存
3. 點「建立器材」
4. **調整庫存**：耗材列表中點數字，可增減（+增加/-減少）
5. **編輯器材**：點「編輯」可修改任何欄位
6. **新增設備個體**：編輯設備類器材時，可新增編號（如 #21、#22）

#### 編輯與刪除

所有頁面都可以修改或刪除記錄：

| 頁面 | 操作 |
|---|---|
| 進出單列表 | 每列有 ✎編輯 和 ✕刪除 |
| 設備列表 | 每列有 ✎編輯 和 ✕刪除 |
| 耗材庫存 | 每列有 ±調整、✎編輯、✕刪除 |
| 異動紀錄 | 每列有 ✎編輯 和 ✕刪除 |
| 設備設置 | 每列有 編輯 和 ✕刪除 |

> ⚠ 所有修改和刪除都會記錄在 audit_log 中（舊值→新值），可事後追溯。

---

### 2.3 自然語言查詢

適用於：想查資料但不想在介面上逐一篩選。

#### 如何使用

在 AI Agent 平台（opencode）中使用 `warehouse-query` 技能，直接用口語問問題。

#### 可以問什麼？

**查設備去向：**
```
鑽機#21 上個月去哪了？
```
→ Agent 會找到鑽機#21，列出它最近的異動紀錄

**查案場設備：**
```
26-023 這個案場還有哪些設備沒回？
```
→ Agent 列出在 26-023 案場且狀態為「在外」的設備

**查耗材庫存：**
```
二重管目前庫存多少？哪些規格低於5支？
```
→ Agent 列出所有二重管規格的庫存，標出低於5支的

**查進出單：**
```
這個月有哪些進出單？
```
→ Agent 列出本月所有進出單

**查報表：**
```
今年哪些案場用最多設備？
```
→ Agent 產生按案號分組的統計

**查故障設備：**
```
現在有什麼設備在送修？
```
→ Agent 列出所有狀態為「待修」的設備

#### 查詢特點

- **可多輪追問**：先問「26-023 有哪些設備」，再問「那其中待修的呢」，Agent 會記住前次條件
- **可用民國年**：「113年8月的進出單」會自動轉換
- **回答附行動建議**：如「有4台在外超過30天，建議追回」
- **查詢不會修改資料**：查詢 Agent 只讀不寫，安全無虞

#### 查詢 vs 進出單 vs 異動紀錄的差別

| 名稱 | 是什麼 | 用途 |
|---|---|---|
| **進出單** | 一張操作憑證（如一張手寫單） | 查某次操作、追溯原始照片 |
| **異動紀錄** | 每台設備的履歷 | 查某設備去過哪些案場 |
| **查詢** | 用自然語言問問題 | 快速得到答案，不用手動篩選 |

> 簡單說：進出單是「收據」，異動紀錄是「流水帳」，查詢是「問問題」。
